C program to print alphabets to a file using separate threads for each alphabet.

It can be compiled as follows:
gcc mthread.c -o mthread -lpthread (lpthread is to include pthread.h).

On execution, it prints 50 randomly generated words to a file.

Currently not done. Some improvements required.
